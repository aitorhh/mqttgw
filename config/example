# Registration ticket in json string format
export APPIOT_REGISTRATION_TICKET='<REGISTRATION_TICKET>'

# CouchDB URL, for example: http://localhost:5984
export APPIOT_COUCHDB_URL='<COUCHDB_URL>'

# If the db uses authentication
export APPIOT_COUCHDB_USER='<COUCHDB_USER>'
export APPIOT_COUCHDB_PASSWORD='<COUCHDB_PASSWORD>'

# Basic MQTT connection information
export APPIOT_MQTT_URL='<MQTT_URL>'
export APPIOT_MQTT_CLIENT_ID='<MQTT_CLIENT_ID>'

# The gateway will listen to <TOPIC_PREFIX>endpoint/objectId/instanceId/resourceId. 
export APPIOT_MQTT_TOPIC_PREFIX='my/custom/topic/'

# If the MQTT broker users authentication
export APPIOT_MQTT_USER='<MQTT_USER'
export APPIOT_MQTT_PASSWORD='<MQTT_PASSWORD>'
